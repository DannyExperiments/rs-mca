# Cycle 70 K3/K4 Ladder Prompt Packet

This packet stages the next exact Packy/Fable attempt after Cycle 69.

Target:

```text
V-CYCLE69-K3-K4-INJECTIVITY-LADDER
W-CYCLE69-SLOT-SUMSET-COLLISION-ENERGY
```

Use `CYCLE70_K3_K4_LADDER_PROMPT.md` as the submitted prompt.

The intended result is one of:

1. a proof or optimized verifier for the `k=3` and `k=4` product-injectivity
   rungs;
2. the first explicit partial collision if a rung fails;
3. a proof of `D <= 155`;
4. an explicit 13-fold collision;
5. a compiled verifier specification for the exact ordered collision count.

