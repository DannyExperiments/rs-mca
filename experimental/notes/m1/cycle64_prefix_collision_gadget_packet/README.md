# Cycle 64 Prefix-Collision Gadget Packet

This packet is the first post-Cycle-63 Fable worker prompt.

Cycle 63 clarified the scalar-apolar block-trade route:

- corrected relative block-collapse is bankable;
- canonical `(K,D)` assignment and dyadic overlap accounting are bankable;
- the hoped block-free residual bound is false;
- the live wall is now partial-pattern / prefix-collision gadget mass.

The prompt targets:

```text
L-MODEL-GJ-PREFIX-COLLISION-GADGET-PARTITION-AND-CHARGE
```

The worker should either prove an exact partition/charge theorem that absorbs
the Role 05 characteristic-17 packet, or produce a stronger falsifier showing
that the scalar-apolar route needs a different invariant.
