# ROLE 04 - Primary-Route Counterpacket Hunter

Append this role prompt after `COMMON_PROMPT.md`.

## Wall

```text
W-LIST-MODEL-GJ-QUOTIENT-CONDITIONED-LOCAL-LIMIT
```

## Objective

Try to kill the model generalized-Jacobian local-limit route.

Search for exact rich fibers after conditioning on:

```text
G_eff
Q_per
```

Use exhaustive small-field reasoning for structure discovery, then attempt to
promote any anomaly to an official-rate or parameterized construction.

## Required Checks

For every candidate packet, verify:

- it is in the minimal scalar stratum `deg A=sigma+1`, `deg B=j`;
- it satisfies the full-coordinate criterion, not merely support counting;
- it is not already represented by the declared quotient `Q_per`;
- it is not a generator-shear artifact;
- it is relevant to finite `T_q=floor(q/2^128)` thresholds if claimed.

## Success Criteria

Output `COUNTERPACKET` if you find an exact official-rate or parameterized
packet whose minimal-stratum scalar list exceeds the finite target or defeats
the proposed residual bound after all declared conditioning.

## Failure Criteria

Output `AUDIT` if all anomalies are toy-only, non-full-coordinate, or already
accounted for by `G_eff/Q_per`.

