# ROLE 04: Relevance Counterpacket Hunter

Try to kill the claim that Cycle84 materially advances the official prize
frontier.

You are not disputing the public finite count unless you find a precise
mathematical inconsistency. Instead, search for reasons the count does not
produce an official MCA counterpacket.

Possible failure modes:

1. wrong field denominator: `Occ(beta)>2^32` is not the native
   `floor(q_line/2^128)` comparison;
2. model reserve `(n=256, sigma=6, j=113)` is outside the official reserve
   frontier;
3. tensor amplification does not preserve the exact `t=1` Role05 conditions;
4. occupied colors do not all correspond to distinct transverse slopes;
5. the packet is quotient/periodic and must be charged to an existing
   exception profile;
6. the construction uses a toy/generated-field model not matching the official
   smooth-domain setting;
7. lower certificate is valid but too small after field normalization.

Return a formal `COUNTERPACKET` if you can invalidate the transfer. If you
cannot, return the strongest audited statement and the remaining exact wall.
