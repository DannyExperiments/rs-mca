# Cycle 66 Sevenfold Product Occupancy Packet

This packet stages the next exact Packy/Fable worker prompt after Cycle 65.

Target:

```text
V-CYCLE65-SEVENFOLD-PRODUCT-OCCUPANCY-VERIFIER
W-MODEL-GJ-SEVENFOLD-POLY-PRODUCT-SET-OCCUPANCY
```

Use this prompt with the current project source snapshot and allow output files
if the harness supports them, so the worker can emit a verifier script or
certificate format.

Files:

- `CYCLE66_SEVENFOLD_PRODUCT_OCCUPANCY_VERIFIER_PROMPT.md`
- `SHA256SUMS.txt`

