# Cycle 71 Optimized Ladder Run Prompt Packet

This packet stages the next exact Packy/Fable attempt after Cycle 70.

Target:

```text
V-CYCLE70-K3-K4-OPTIMIZED-LADDER-RUN
W-CYCLE69-SLOT-SUMSET-COLLISION-ENERGY
```

Use `CYCLE71_OPTIMIZED_LADDER_RUN_PROMPT.md` as the submitted prompt.

The intended output is executable progress:

1. optimized exact code for `k=3/k=4` product-injectivity;
2. a proved structural shortcut that survives the Cycle 70 counterexample;
3. an explicit partial collision;
4. or a decisive energy-count verifier plan with certificate schema.

